version: 2

models:
  - name: snowplow_web_users_manifest
    description: '{{ doc("table_users_manifest") }}'
    columns:
      - name: domain_userid
        description: '{{ doc("col_domain_userid") }}'
        tags:
          - primary-key
        tests:
          - unique
          - not_null
